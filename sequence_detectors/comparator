module comparator #(parameter N = 8)(
    input  [N-1:0] a,
    input  [N-1:0] b,
    output eq,
    output gt,
    output lt
);
    assign eq = (a == b);
    assign gt = (a > b);
    assign lt = (a < b);
endmodule
